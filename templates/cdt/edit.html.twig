{% extends 'base.html.twig' %}

{% block title %}Edit CDT{% endblock %}

{% block body %}
    <h1>Edit CDT</h1>

    {{ include('cdt/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('c_d_t_index') }}">back to list</a>

    {{ include('cdt/_delete_form.html.twig') }}
{% endblock %}
